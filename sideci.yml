linter:
  code_sniffer:
    options:
      standard: rulesets/phpcs.xml
      extensions: php
      ignore: vendor
  phpmd:
    options:
      rule: rulesets/phpmd.xml
      suffixes: .php
      ignore: vendor
  eslint:
    options:
      config: ".eslintrc"
